# Secrets
.env

# Runtime config (contains host IP, subnets — edit your own copy)
config.yml

# Runtime state (peer records, IP pool, cluster subnets)
.roundtable/

# Per-agent persistent storage (configs, volumes)
.agents/

# Build artifacts
__pycache__/
*.pyc
node_modules/
.venv/

# Editor
.vscode/
.idea/
*.swp
*.swo